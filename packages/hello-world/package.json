{
    "name": "hello-world",
    "version": "0.0.1",
    "license": "MIT",
    "private": true,
    "type": "module",
    "scripts": {
        "clean": "rm -rf __lwr_cache__",
        "dev": "lwr serve --port 3000",
        "start": "lwr serve --port 3000 --mode prod",
        "start:compat": "lwr serve --port 3000 --mode compat",
        "start:prod-compat": "lwr serve --port 3000 --mode prod-compat"
    },
    "dependencies": {
        "lwc": "~2.2.0",
        "lwr": "0.5.5"
    },
    "engines": {
        "node": ">=14.15.4 <17"
    }
}
